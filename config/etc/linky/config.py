root_path = "/mine/.layers"
# Key: Destination, Value: Source
config = {
    # If you would like to bring your own config for bash you can do so like this:
    # The key defines the destination you want to have your final conf file as a symlink
    # The Value has to point to you own config file's path in your persistance storege
    # "/root/.bashrc": "/mine/.layers/root/.bashrc",
}